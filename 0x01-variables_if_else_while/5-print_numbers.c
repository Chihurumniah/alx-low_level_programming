#include <stdio.h>
/**
 * main - Entry poit of the program
 *
 * Return: Return 0 if successful
 */
int main(void)
{
	int n;

	for (n = 0; n < 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
