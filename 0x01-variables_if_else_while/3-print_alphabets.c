#include <stdio.h>

/**
 * main - Entry point of the program
 * @void: Null valeu
 *
 * Return: Return 0 if success
 */
int main(void)
{
	char c, c;

	for (c = "a"; c <= "z"; c++)
		putchar(c);
	for (b = "A"; b <= "Z"; b++)
		putchar(b);
	putchar("\n");
	return (0);
}
